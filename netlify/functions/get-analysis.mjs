// get-analysis.mjs — token-scoped Analyze Fit polling
const SUPABASE_URL=process.env.SUPABASE_URL;const SUPABASE_KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;const CORS={'Content-Type':'application/json','Access-Control-Allow-Origin':'*','Access-Control-Allow-Methods':'GET, OPTIONS','Access-Control-Allow-Headers':'Content-Type, Authorization'};function sbH(){return{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json'}}async function sbGet(path){const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{headers:sbH()});if(!r.ok)throw new Error(`Supabase GET: ${(await r.text()).slice(0,200)}`);return r.json()}async function verifySession(h){if(!h?.startsWith('Bearer '))return null;const t=h.slice(7).trim();if(!t)return null;try{const rows=await sbGet(`client_sessions?session_token=eq.${encodeURIComponent(t)}&revoked=eq.false&select=email,expires_at&limit=1`);if(!rows[0]||new Date(rows[0].expires_at)<new Date())return null;return rows[0].email.toLowerCase().trim()}catch{return null}}async function verifyViewToken(t){if(!t)return null;try{const rows=await sbGet(`demo_snapshots?view_token=eq.${encodeURIComponent(t)}&status=eq.complete&select=requester_email&limit=1`);return rows[0]?.requester_email?.toLowerCase().trim()||null}catch{return null}}export const handler=async event=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers:CORS,body:''};if(event.httpMethod!=='GET')return{statusCode:405,headers:CORS,body:JSON.stringify({error:'GET only'})};const q=event.queryStringParameters||{};let accountEmail=await verifySession(event.headers?.authorization||event.headers?.Authorization||'');if(!accountEmail&&q.view_token)accountEmail=await verifyViewToken(q.view_token);if(!accountEmail)return{statusCode:401,headers:CORS,body:JSON.stringify({error:'UNAUTHORIZED'})};const ae=encodeURIComponent(accountEmail);let rows;if(q.id)rows=await sbGet(`opportunity_analyses?id=eq.${encodeURIComponent(q.id)}&account_email=eq.${ae}&limit=1`);else if(q.opportunityId)rows=await sbGet(`opportunity_analyses?account_email=eq.${ae}&opportunity_id=eq.${encodeURIComponent(q.opportunityId)}&profile_version=eq.0&limit=1`);else return{statusCode:400,headers:CORS,body:JSON.stringify({error:'id or opportunityId required'})};if(!rows.length)return{statusCode:404,headers:CORS,body:JSON.stringify({error:'Not found'})};return{statusCode:200,headers:CORS,body:JSON.stringify(rows[0])}};
